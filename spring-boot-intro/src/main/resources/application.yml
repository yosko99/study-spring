server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost/spring-boot
    username: postgres
    password: example
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true